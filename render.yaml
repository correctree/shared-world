services:
  - type: web
    name: shared-world-colyseus
    runtime: node
    plan: free
    buildCommand: npm install --no-audit --no-fund
    startCommand: npm run server:start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "22"
